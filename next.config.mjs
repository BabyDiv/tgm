/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
};

export default nextConfig;

// import nextTranslate from 'next-translate';

// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
//   swcMinify: true,

//   // Пример: если используешь SCSS
//   sassOptions: {
//     includePaths: ['./src/styles'],
//   },

//   // Пример: поддержка изображений
//   images: {
//     domains: ['example.com'],
//   },

//   // Другие настройки можно добавлять сюда
// };

// export default nextTranslate(nextConfig);

